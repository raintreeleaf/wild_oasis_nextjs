//The three dots are catch all for urls
//The line below implicitly import and export, all in one line. Refer to Gemini for details.
export { GET, POST } from "@/app/_lib/auth";
//The server needs to handle get, post request from google.
